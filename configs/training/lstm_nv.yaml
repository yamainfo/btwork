data_config: 'mode_1'
model: 'LSTM_nv'

name: 'lstm'
max_epochs: 700
use_volume: False

hyperparams:
    optimizer: 'adam'
    lr: 0.001
    lr_step_size: 100
    lr_gamma: 0.5
    weight_decay: 0.000005